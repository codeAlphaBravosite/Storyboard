# Create a basic package.json template and write it to the project directory

package_json_content = {

    "name": "storyboard-app",

    "version": "1.0.0",

    "private": True,

    "homepage": "https://<username>.github.io/<repository-name>",  # Replace with actual username and repo

    "dependencies": {

        "react": "^18.2.0",

        "react-dom": "^18.2.0",

        "react-scripts": "5.0.1",

        "typescript": "^4.9.5"

    },

    "scripts": {

        "start": "react-scripts start",

        "build": "react-scripts build",

        "test": "react-scripts test",

        "eject": "react-scripts eject",

        "predeploy": "npm run build",

        "deploy": "gh-pages -d build"

    },

    "devDependencies": {

        "gh-pages": "^5.0.0"

    }

}



# Define the path for the package.json file

package_json_path = os.path.join(main_dir, 'package.json')



# Write the JSON content to the file

import json

with open(package_json_path, 'w') as f:

    json.dump(package_json_content, f, indent=2)



# Confirm creation

os.listdir(main_dir)
